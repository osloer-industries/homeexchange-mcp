# This project stores its local browser session in git-ignored session.json.
# Do not put HomeExchange credentials or session tokens in environment files.

# CI-only example. Configure the real value as a GitHub Actions secret.
SONAR_TOKEN=replace-with-a-local-sonarqube-cloud-token
